algoritmo "atividade - correção"
var
contador: inteiro

inicio
contador <- 1
 enquanto (contador <= 10) faca
  escreval (contador, "x 8 = ", contador*8)
   contador <- contador + 1
 fimEnquanto

fimalgoritmo